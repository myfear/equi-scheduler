[allowlist]
  description = "Global Allowlist"

  # Ignore based on any subset of the file path
  paths = [
  # Ignore all Test *.java files 
  '''(?:^|/)test/java/org/acme/.*\.java$'''
      # Ignore all files named import.sql
  '''(?:^|/)import\.sql$'''
  ]